import styled from "styled-components";

export const Button = styled.button`
  position: fixed;
  right: 20px;
  bottom: 20px;
  z-index: 20;
  background-color: var(--Primary);
  min-width: 60px;
  min-height: 60px;
  border: none;
  border-radius: 100%;
  cursor: pointer;
`;
